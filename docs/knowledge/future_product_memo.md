# Future Product Memo

Task-Demo-Align reinforces one planning rule: the engineering demo should stay aligned with firmware semantics and SDK capability, but product strategy remains elsewhere.

Implications:

- future auto-adjust / manual override UX should continue to live in product-side app layers
- future attribution UX should continue to live in product-side app layers
- the demo may show lightweight debug mirrors of those states only when a clean, already-available adapter exists
- no future task should treat CH341 as the default path for new SonicWave protocol capability
